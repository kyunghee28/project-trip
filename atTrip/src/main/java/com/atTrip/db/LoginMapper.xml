<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="lo">
	<select id="gin" resultType="joinvo">
		select m_no from member where m_email = #{m_email} and m_pwd=#{m_pwd}	
	</select>
</mapper>